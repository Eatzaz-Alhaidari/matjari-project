# النواقص والميزات المطلوبة - مشروع متجري

## نظرة عامة

هذا الملف يحتوي على قائمة شاملة بالنواقص والميزات المطلوبة في المشروع قبل البدء في بناء الفرونت إند.

---

## 🔴 النواقص الحرجة (يجب إضافتها قبل الفرونت إند)

### 1. نظام السحب (Withdrawal System) ⚠️ **مهم جداً**

**الوضع الحالي:**
- ✅ يوجد جدول `wallets` مع حقل `withdrawn_amount`
- ✅ يوجد جدول `wallet_transactions` مع نوع `withdrawal`
- ❌ **لا يوجد Controller أو Routes لسحب الأموال**
- ❌ **لا يوجد واجهة للبائع لطلب السحب**
- ❌ **لا يوجد نظام موافقة المسؤول على طلبات السحب**

**ما يجب إضافته:**
- [ ] جدول `withdrawal_requests` (id, wallet_id, amount, status, bank_account, notes, admin_response)
- [ ] Controller للبائع: `Vendor/WithdrawalController` (index, create, store)
- [ ] Controller للمسؤول: `Admin/WithdrawalController` (index, approve, reject)
- [ ] Routes للبائع: `/vendor/withdrawals`
- [ ] Routes للمسؤول: `/admin/withdrawals`
- [ ] Views للبائع: طلب سحب، قائمة طلبات السحب
- [ ] Views للمسؤول: مراجعة وموافقة/رفض طلبات السحب
- [ ] إعدادات: الحد الأدنى للسحب (`min_withdrawal_amount`)

---

### 2. نظام العمولات التلقائي (Automatic Commission System) ⚠️ **مهم جداً**

**الوضع الحالي:**
- ✅ يوجد إعداد `commission_rate` في جدول `settings` (10%)
- ✅ يتم حساب العمولة في التقارير المالية
- ❌ **لا يوجد نظام تلقائي لخصم العمولة عند تسليم الطلب**
- ❌ **لا يوجد نظام لإضافة الأرباح للمحفظة بعد خصم العمولة**

**ما يجب إضافته:**
- [ ] Service: `CommissionService` لحساب العمولة تلقائياً
- [ ] Event Listener: عند تغيير حالة الطلب إلى `delivered`
- [ ] حساب العمولة: `total_amount * commission_rate`
- [ ] إضافة الأرباح للمحفظة: `total_amount - commission`
- [ ] تسجيل المعاملة في `wallet_transactions`
- [ ] تحديث `total_earnings` و `balance` في `wallets`

**مثال الكود المطلوب:**
```php
// في OrderObserver أو Event Listener
if ($order->status === 'delivered' && $order->payment_status === 'paid') {
    $commissionRate = Setting::where('key', 'commission_rate')->value('value') / 100;
    $commission = $order->total_amount * $commissionRate;
    $vendorEarnings = $order->total_amount - $commission;
    
    // إضافة للمحفظة
    $wallet = Wallet::firstOrCreate(['user_id' => $order->store->user_id]);
    $wallet->increment('balance', $vendorEarnings);
    $wallet->increment('total_earnings', $vendorEarnings);
    
    // تسجيل المعاملة
    WalletTransaction::create([...]);
}
```

---

### 3. نظام الإشعارات التلقائي (Automatic Notifications) ⚠️ **مهم**

**الوضع الحالي:**
- ✅ يوجد جدول `notifications`
- ✅ يوجد `TestNotification` فقط
- ❌ **لا يوجد إشعارات تلقائية عند الأحداث المهمة**

**ما يجب إضافته:**

#### إشعارات للبائع:
- [ ] إشعار عند طلب جديد
- [ ] إشعار عند تغيير حالة الطلب
- [ ] إشعار عند تقييم جديد لمنتج
- [ ] إشعار عند طلب إرجاع
- [ ] إشعار عند موافقة/رفض طلب السحب
- [ ] إشعار عند نفاد المخزون

#### إشعارات للعميل:
- [ ] إشعار عند قبول الطلب
- [ ] إشعار عند شحن الطلب
- [ ] إشعار عند تسليم الطلب
- [ ] إشعار عند إلغاء الطلب
- [ ] إشعار عند موافقة/رفض طلب الإرجاع

#### إشعارات للمسؤول:
- [ ] إشعار عند شكوى جديدة
- [ ] إشعار عند طلب إرجاع جديد
- [ ] إشعار عند طلب سحب جديد
- [ ] إشعار عند منتج جديد يحتاج موافقة (إن كان مفعل)

**ما يجب إضافته:**
- [ ] Notification Classes: `NewOrderNotification`, `OrderStatusChangedNotification`, إلخ
- [ ] Event Listeners: `OrderCreated`, `OrderStatusChanged`, إلخ
- [ ] Queue Jobs: لإرسال الإشعارات (اختياري)

---

### 4. نظام الموافقة على المنتجات (Product Approval System)

**الوضع الحالي:**
- ✅ يوجد إعداد `auto_approve_products` في `settings`
- ❌ **لا يوجد نظام موافقة فعلي**
- ❌ **المنتجات تُنشأ مباشرة بدون مراجعة**

**ما يجب إضافته:**
- [ ] حقل `approval_status` في جدول `products` (pending, approved, rejected)
- [ ] عند إنشاء منتج جديد: إذا `auto_approve_products = 0`، ضع الحالة `pending`
- [ ] Controller للمسؤول: موافقة/رفض المنتجات
- [ ] View للمسؤول: قائمة المنتجات المعلقة
- [ ] Route: `/admin/products/pending`
- [ ] إشعار للمسؤول عند منتج جديد يحتاج موافقة
- [ ] إشعار للبائع عند موافقة/رفض المنتج

---

### 5. نظام تقييم المتاجر (Store Reviews)

**الوضع الحالي:**
- ✅ يوجد تقييم للمنتجات فقط
- ❌ **لا يوجد تقييم للمتاجر**

**ما يجب إضافته:**
- [ ] جدول `store_reviews` (id, user_id, store_id, rating, comment, status)
- [ ] Model: `StoreReview`
- [ ] Controller: `StoreReviewController` (API)
- [ ] Routes: `POST /api/stores/{store}/reviews`
- [ ] إحصائيات التقييمات في لوحة تحكم البائع
- [ ] عرض التقييمات في صفحة المتجر

---

## 🟡 النواقص المهمة (يُنصح بإضافتها)

### 6. حساب تكلفة الشحن الديناميكي

**الوضع الحالي:**
- ✅ تكلفة الشحن ثابتة: 1000 دينار
- ❌ **لا يوجد حساب ديناميكي حسب الوزن أو المسافة**

**ما يجب إضافته:**
- [ ] جدول `shipping_zones` (id, name, base_cost, cost_per_kg)
- [ ] جدول `shipping_rates` (id, zone_id, min_weight, max_weight, cost)
- [ ] Service: `ShippingCalculatorService`
- [ ] حساب التكلفة حسب:
  - الوزن الإجمالي للطلب
  - المدينة/المنطقة
  - المسافة (اختياري)

---

### 7. نظام الكوبونات والخصومات المتقدم

**الوضع الحالي:**
- ✅ يوجد نظام خصومات أساسي
- ❌ **لا يوجد تطبيق تلقائي للخصومات عند الطلب**
- ❌ **لا يوجد نظام كوبونات للعملاء**

**ما يجب إضافته:**
- [ ] تطبيق الخصم تلقائياً عند استخدام الكود في الطلب
- [ ] التحقق من:
  - صلاحية الكود
  - الحد الأدنى لمبلغ الطلب
  - عدد مرات الاستخدام
  - المنتجات المطبق عليها
- [ ] API Endpoint: `POST /api/orders/apply-discount`
- [ ] حساب السعر النهائي بعد الخصم

---

### 8. نظام إدارة المخزون المتقدم

**الوضع الحالي:**
- ✅ يوجد تنبيهات للمخزون القليل
- ❌ **لا يوجد تنبيهات تلقائية عند نفاد المخزون**
- ❌ **لا يوجد سجل حركات المخزون**

**ما يجب إضافته:**
- [ ] جدول `stock_movements` (id, product_id, type, quantity, reason, reference_id)
- [ ] تسجيل جميع حركات المخزون:
  - عند البيع
  - عند الإرجاع
  - عند التعديل اليدوي
  - عند الاستيراد
- [ ] إشعارات تلقائية عند:
  - نفاد المخزون
  - وصول المخزون للحد الأدنى
- [ ] تقرير حركات المخزون

---

### 9. نظام المتابعة والتنبيهات للطلبات

**الوضع الحالي:**
- ✅ يوجد حالات للطلبات
- ❌ **لا يوجد نظام متابعة للطلبات المتأخرة**
- ❌ **لا يوجد تنبيهات للطلبات المعلقة لفترة طويلة**

**ما يجب إضافته:**
- [ ] Cron Job: للتحقق من الطلبات المتأخرة
- [ ] تعريف "متأخر": طلب في حالة `processing` لأكثر من 3 أيام
- [ ] إشعارات تلقائية:
  - للبائع: طلب متأخر
  - للمسؤول: طلب متأخر
- [ ] Dashboard Widget: عدد الطلبات المتأخرة

---

### 10. نظام إدارة العناوين

**الوضع الحالي:**
- ✅ العنوان يُدخل كنص في الطلب
- ❌ **لا يوجد نظام حفظ العناوين للعملاء**

**ما يجب إضافته:**
- [ ] جدول `addresses` (id, user_id, label, address, city, phone, is_default)
- [ ] Model: `Address`
- [ ] Controller: `AddressController` (API)
- [ ] Routes: CRUD للعناوين
- [ ] في صفحة الطلب: اختيار من العناوين المحفوظة أو إضافة جديد

---

### 11. نظام البحث والفلترة المتقدم

**الوضع الحالي:**
- ✅ يوجد بحث بسيط
- ❌ **لا يوجد فلترة متقدمة**

**ما يجب إضافته للفرونت إند:**
- [ ] فلترة حسب:
  - السعر (من - إلى)
  - الفئة
  - المتجر
  - التقييم
  - العلامة التجارية
  - التوفر (في المخزون)
- [ ] ترتيب حسب:
  - الأحدث
  - الأقل سعراً
  - الأعلى سعراً
  - الأعلى تقييماً
  - الأكثر مبيعاً
- [ ] API Endpoints للبحث والفلترة

---

### 12. نظام سلة التسوق (Shopping Cart)

**الوضع الحالي:**
- ❌ **لا يوجد نظام سلة تسوق**
- ❌ **الطلبات تُنشأ مباشرة**

**ما يجب إضافته:**
- [ ] جدول `carts` (id, user_id, created_at, updated_at)
- [ ] جدول `cart_items` (id, cart_id, product_id, quantity)
- [ ] Model: `Cart`, `CartItem`
- [ ] Controller: `CartController` (API)
- [ ] Routes:
  - `GET /api/cart` - عرض السلة
  - `POST /api/cart/items` - إضافة منتج
  - `PUT /api/cart/items/{id}` - تحديث الكمية
  - `DELETE /api/cart/items/{id}` - حذف منتج
  - `POST /api/cart/checkout` - إنشاء الطلب من السلة

---

### 13. نظام المفضلة (Wishlist)

**الوضع الحالي:**
- ❌ **لا يوجد نظام مفضلة**

**ما يجب إضافته:**
- [ ] جدول `wishlists` (id, user_id, product_id, created_at)
- [ ] Model: `Wishlist`
- [ ] Controller: `WishlistController` (API)
- [ ] Routes:
  - `GET /api/wishlist` - عرض المفضلة
  - `POST /api/wishlist/{product}` - إضافة للمفضلة
  - `DELETE /api/wishlist/{product}` - حذف من المفضلة

---

### 14. نظام المقارنة (Product Comparison)

**الوضع الحالي:**
- ❌ **لا يوجد نظام مقارنة**

**ما يجب إضافته:**
- [ ] Session-based comparison (أو جدول `product_comparisons`)
- [ ] API: إضافة/حذف منتج للمقارنة
- [ ] Frontend: صفحة المقارنة

---

## 🟢 تحسينات إضافية (اختيارية)

### 15. نظام التقييمات المتقدم

- [ ] تقييمات مع صور
- [ ] تقييمات مفيدة/غير مفيدة
- [ ] رد البائع على التقييمات
- [ ] تصفية التقييمات حسب التقييم

### 16. نظام الإعلانات المتقدم

- [ ] إحصائيات مفصلة (CTR, Conversion Rate)
- [ ] استهداف حسب الفئة/المنطقة
- [ ] نظام المزادات للإعلانات

### 17. نظام التقارير المتقدم

- [ ] تصدير التقارير PDF/Excel
- [ ] تقارير مخصصة
- [ ] تقارير مقارنة بين الفترات

### 18. نظام الأمان

- [ ] Two-Factor Authentication (2FA)
- [ ] تسجيل محاولات الدخول الفاشلة
- [ ] حظر IP تلقائي بعد محاولات فاشلة متعددة

### 19. نظام النسخ الاحتياطي

- [ ] نسخ احتياطي تلقائي يومي
- [ ] استعادة من النسخ الاحتياطي
- [ ] إشعارات عند فشل النسخ الاحتياطي

### 20. نظام الدعم الفني المتقدم

- [ ] نظام التذاكر (Tickets)
- [ ] دعم مباشر (Live Chat)
- [ ] قاعدة معرفة (Knowledge Base)

---

## 📋 أولويات التنفيذ

### المرحلة 1 (قبل الفرونت إند) - **حرجة**:
1. ✅ نظام السحب (Withdrawal System)
2. ✅ نظام العمولات التلقائي
3. ✅ نظام الإشعارات التلقائي
4. ✅ نظام الموافقة على المنتجات

### المرحلة 2 (مع الفرونت إند) - **مهمة**:
5. ✅ نظام تقييم المتاجر
6. ✅ حساب تكلفة الشحن الديناميكي
7. ✅ نظام الكوبونات والخصومات المتقدم
8. ✅ نظام سلة التسوق
9. ✅ نظام المفضلة
10. ✅ نظام البحث والفلترة المتقدم

### المرحلة 3 (بعد الفرونت إند) - **تحسينات**:
11. ✅ نظام إدارة المخزون المتقدم
12. ✅ نظام المتابعة والتنبيهات
13. ✅ نظام إدارة العناوين
14. ✅ باقي التحسينات

---

## 📝 ملاحظات مهمة

1. **نظام العمولات:** يجب أن يكون تلقائياً عند تسليم الطلب، وليس يدوياً
2. **نظام السحب:** يجب أن يكون آمن مع موافقة المسؤول
3. **الإشعارات:** يجب أن تكون تلقائية عند جميع الأحداث المهمة
4. **الموافقة على المنتجات:** مهم للتحكم في جودة المنتجات المعروضة

---

**تاريخ الإنشاء:** 2026
**آخر تحديث:** 2026
